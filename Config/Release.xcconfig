#include "Base.xcconfig"

// Xcode otherwise injects com.apple.security.get-task-allow, which lets a
// debugger attach to a shipped build and makes notarization fail.
CODE_SIGN_INJECT_BASE_ENTITLEMENTS = NO
