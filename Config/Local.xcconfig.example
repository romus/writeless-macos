// Copy to Config/Local.xcconfig (gitignored) or run `make setup-signing`.
// Signing Debug builds with a stable identity keeps the macOS Microphone
// permission from resetting on every rebuild.
CODE_SIGN_IDENTITY = Apple Development
DEVELOPMENT_TEAM = XXXXXXXXXX
